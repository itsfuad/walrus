

let a := 78;

type Point struct{
    x: i32,
    y: i32
};

type Int i32;

let x : Int = 3;

let point : Point = @Point{ x: 2, y: 4 };

type Array []i32;

let arr : Array = [1,2,3];

//let pi := PI; //declared in the main.go